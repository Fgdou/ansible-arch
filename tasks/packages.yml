---
- name: Installing aur packages
  become: yes
  become_user: aur_builder
  kewlfft.aur.aur:
    name: "{{ user_packages }}"
    use: yay
    state: present
- name: Enabling services
  become: true
  service:
    name: "{{ item }}"
    enabled: true
  loop: "{{ service_list }}"